--- a/src/screens/payment/EnhancedPaymentScreen.tsx
+++ b/src/screens/payment/EnhancedPaymentScreen.tsx
@@ -303,7 +303,12 @@ const EnhancedPaymentScreen: React.FC = () => {
               } else if (methodId === 'qrCode') {
                 setShowQRModal(true);
                 generateQRCode();
               } else if (methodId === 'card') {
-                setShowSumUpModal(true);
+                // Validate customer info before triggering SumUp
+                if (\!isFormValid) {
+                  Alert.alert('Required Information', 'Please enter valid customer name and email address.');
+                  return;
+                }
+                setShowSumUpModal(true);
               } else if (methodId === 'applePay') {
                 Alert.alert('Apple Pay', 'Hold near reader and confirm with Touch ID or Face ID.');
               } else if (methodId === 'googlePay') {
@@ -322,8 +327,13 @@ const EnhancedPaymentScreen: React.FC = () => {
         setShowQRModal(true);
         generateQRCode();
       } else if (methodId === 'card') {
-        // Card payment handling - could show card reader interface
-        setShowSumUpModal(true);
+        // Card payment handling - validate customer info first
+        // Validate customer info before triggering SumUp
+        if (\!isFormValid) {
+          Alert.alert('Required Information', 'Please enter valid customer name and email address.');
+          return;
+        }
+        setShowSumUpModal(true);
       } else if (methodId === 'applePay') {
         // Apple Pay handling
         Alert.alert('Apple Pay', 'Hold near reader and confirm with Touch ID or Face ID.');
