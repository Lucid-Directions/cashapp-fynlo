# CRITICAL SECURITY WARNING: 
# NEVER commit real credentials to version control!
# Copy this file to .env.local and add your actual values there
# Make sure .env.local is in .gitignore

# Backend API Configuration
VITE_API_URL=https://api.fynlo.co.uk/api/v1
VITE_WEBSOCKET_URL=wss://api.fynlo.co.uk/ws

# Supabase Configuration (for authentication)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Environment
VITE_ENV=production

# IMPORTANT: The following sensitive credentials should NEVER be stored in frontend code
# They must be kept secure on the backend only:
# - Payment provider API keys (SumUp, Stripe, Square)
# - Database connection strings
# - Redis connection strings
# - DigitalOcean Spaces credentials
# - Supabase service role keys
# - Any other secret keys or tokens