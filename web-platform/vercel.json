{
  "framework": "vite",
  "buildCommand": "cd .. && npm install && cd web-platform && npm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}